import Header from "../components/header";

const HeaderContainer = () => {
  return (
    <Header>
      <Header.Container>
        <Header.Title />
        <Header.Icons />
      </Header.Container>
    </Header>
  );
};

export default HeaderContainer;
